<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.tabeyo.mapper.TimeFeedAttachMapper">
<!-- 잘못 업로드된 파일들 삭제 -->
	 <!--  <select id="getOldFiles" resultType="com.tabeyo.domain.TimeFeedAttachVO">
	  	SELECT 	* 
	  	FROM 	tbl_timefeed_attach
	  	WHERE 	uploadpath = TO_CHAR(SYSDATE - 1, 'yyyy\mm\dd')
	  </select> -->
	  
	  <!-- 첨부 파일 전체 삭제 -->
	 <!--  <delete id="deleteAll">
	  	DELETE FROM tbl_timefeed_attach  WHERE fdNo = #{fdNo}
	  </delete> -->
	  
	  <!-- 첨부 파일 하나 삭제 -->
	<!--   <delete id="delete">
	  	DELETE FROM tbl_timefeed_attach  WHERE uuid = #{uuid}
	  </delete> -->
	  
	  <!-- 첨부 파일 추가 -->
<!-- 	  <insert id="insert">
		INSERT INTO tbl_timefeed_attach(uuid, uploadPath, fileName, filetype, fdNo)
		VALUES( #{uuid}, #{uploadPath}, #{fileName}, #{fileType}, #{fdNo})
	  </insert> -->
	  
	  <!-- 첨부 파일  조회  -->
	<!--   <select id="findByBno" resultType="com.tabeyo.domain.TimeFeedAttachVO">
	  	SELECT * FROM tbl_timefeed_attach WHERE fdNo = #{fdNo}
	  </select> -->
</mapper>